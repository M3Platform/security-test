USE_REDIS_FOR_SHARES=false
REDIS_URL=redis://localhost:6379

USE_GCS_AUDIT_LOGGING=false
GCS_AUDIT_BUCKET=dev-audit-bucket
GCP_PROJECT_ID=your-project-id

# Optional: path to a GCP service account key JSON for local testing.
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
